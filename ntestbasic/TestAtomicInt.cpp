#include "stdafx.h"
using namespace vl;
using namespace vl::unittest;

TEST_CASE(TestAtomicInt)
{
	// 	nAtomicInt num;
	// 	TEST_ASSERT(num.Count() != NULL);
	// 	TEST_ASSERT(*num.Count() == 1);
	//
	// 	nAtomicInt num2 = num;
	// 	TEST_ASSERT(*num2.Count() == 2);
	
	
	
}