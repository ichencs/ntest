#include "stdafx.h"
using namespace vl;

TEST_CASE(TestNumberTrans)
{
	int number = 65536;
	char buffer[100] = {0};
	// 	ITOA_S(number, buffer, sizeof(buffer) / sizeof(*buffer), 10);
	char buffer2[2] = { 0 };
	// 	ITOA_S(number, buffer2, sizeof(buffer2) / sizeof(*buffer2), 16);
	
}